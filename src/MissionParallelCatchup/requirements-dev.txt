# Test-only dependencies. The runtime pins live in Dockerfile.jobmonitor and are
# installed a second time by the chart's sourceConfigMap path -- see
# tests/contract/test_dependency_pins.py, which keeps those two in step and
# checks that the major this suite imports is the major the image ships.
#
# The kubernetes client is listed because the contract tests build real V1*
# objects: the suite must import the same major the image installs.
pytest~=9.1
pytest-asyncio~=1.4
kubernetes~=36.0
aiohttp~=3.14
prometheus-client~=0.19
