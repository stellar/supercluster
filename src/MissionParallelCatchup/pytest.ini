[pytest]
# The monitor and collector are plain modules in the parent directory, not an
# installed package, so the suite needs both on the path: `.` for job_monitor /
# log_collector, `tests` for the fake-cluster harness.
pythonpath = . tests
testpaths = tests
